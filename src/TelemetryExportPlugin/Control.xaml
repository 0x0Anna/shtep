<UserControl x:Class="TelemetryExportPlugin.Control"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="500">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <TextBlock Text="Telemetry Export" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

            <TextBlock Text="Temp directory (in-progress .partial files)" Margin="0,8,0,2"/>
            <TextBox x:Name="TempDirBox" TextChanged="TempDirBox_TextChanged"/>

            <TextBlock Text="Output directory (completed .tsv/.meta.json pairs)" Margin="0,8,0,2"/>
            <TextBox x:Name="OutputDirBox" TextChanged="OutputDirBox_TextChanged"/>

            <TextBlock x:Name="PathWarningText" Foreground="OrangeRed" TextWrapping="Wrap" Margin="0,4,0,0"/>

            <TextBlock Text="Sample rate (Hz)" Margin="0,8,0,2"/>
            <TextBox x:Name="SampleRateBox" TextChanged="SampleRateBox_TextChanged"/>

            <CheckBox x:Name="PurgeCheckBox" Content="Purge incomplete recordings on startup (instead of salvaging)"
                      Margin="0,12,0,0" Checked="PurgeCheckBox_Changed" Unchecked="PurgeCheckBox_Changed"/>

            <TextBlock Text="Pit-lane debounce (ms, circuit only)" Margin="0,12,0,2"/>
            <TextBox x:Name="PitDebounceBox" TextChanged="PitDebounceBox_TextChanged"/>

            <TextBlock Text="Heuristic discontinuity speed threshold (km/h)" Margin="0,12,0,2"/>
            <TextBox x:Name="HeuristicSpeedBox" TextChanged="HeuristicSpeedBox_TextChanged"/>

            <TextBlock Text="Discontinuity detection" Margin="0,12,0,2"/>
            <ComboBox x:Name="DiscontinuityModeCombo" SelectionChanged="DiscontinuityModeCombo_SelectionChanged"/>

            <TextBlock Text="Rewind handling" Margin="0,12,0,2"/>
            <ComboBox x:Name="RewindHandlingCombo" SelectionChanged="RewindHandlingCombo_SelectionChanged"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
